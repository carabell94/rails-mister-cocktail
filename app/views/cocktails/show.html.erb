<h2 class="ml-3"><%= @cocktail.name %></h2>

<h4 class='mt-3 ml-3'>Doses</h4>
<ul>
  <% @cocktail.doses.each do |dose| %>
    <li><%= dose.description %> <%= dose.ingredient.name %> (<%= link_to 'Delete', dose_path(dose), method: :delete, data: {confirm: "Are you sure you wish to delete #{dose.description} #{dose.ingredient.name}?"} %>)</li>
  <% end %>
</ul>

<div class="ml-3">
  <%= link_to 'Add a Dose', new_cocktail_dose_path(@cocktail), class: 'btn btn-light' %><br>
  <%= link_to 'Back', cocktails_path, class: 'btn btn-light mt-3' %>
</div>
